- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc:
    ~/.vimrc:
    ~/.gitconfig:

# - create:
#     - ~/Arbeit

# - shell:
#   - [git submodule update --init --recursive, Installing submodules]
