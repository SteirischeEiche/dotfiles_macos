- defaults:
    link:
      relink: true

- clean: ['~']

- link:
#     ~/.vim: vim
    ~/.zshrc: zshrc
    ~/.vimrc: vimrc

# - create:
#     - ~/Arbeit

# - shell:
#   - [git submodule update --init --recursive, Installing submodules]
