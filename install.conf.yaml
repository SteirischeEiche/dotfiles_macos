- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc:
    ~/.vimrc:
    ~/.gitconfig:
    ~/.zprofile:

- create:
    - ~/GitHub

- shell:
    -
      command: ./setup_homebrew.zsh
      stdin: true
      stdout: true
      stderr: true
